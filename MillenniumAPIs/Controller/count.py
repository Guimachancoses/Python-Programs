def contar_registros(data):

    # Contando a quantidade de registros no array 'rows'
    count = len(data)
    return count

# Exemplo de uso
# data = {
#     "rows": [1, 2, 3, 4, 5]  # Substitua por seu array real
# }

# quantidade = contar_registros(data)
# print(f"A quantidade de registros é: {quantidade}")
